一、作业：
1.复习PageObject模式
    对象库层+case层+page层+公共类
    对象库：将元素定位表达式储存在这个层，方便维护
    page层：需要将前端操作，按照业务封装成不同方法。例如：下单操作→def order():
        selenium查找元素、selenium操作，都写在这。但要注意，这里一般按照页面维度来进行编写。例如：
        登录、获取优惠券的按钮均在“首页”，故：首页page.py中应该有 func登录() 和 func_领取优惠券()
    case层：使用“业务封装层”中写好的方法，拼装出一个完整的测试场景 并且 添加检查点
    公共层：如果框架中，需要进行日志记录、数据库连接、加解密、复杂计算等功能，均在这个层来编写

2.复习接口测试内容
    测试数据的准备
    测试脚本的开发
    测试执行
    测试结果分析汇报
    测试集的维护优化
3.复习python基础语法
    函数、对象
    类、集成
    内置函数